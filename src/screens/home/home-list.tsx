import React from 'react';
import { StyleSheet, View } from 'react-native';

function HomeListScreen() {
  return <View style={styles.wrapper}>HomeListScreen</View>;
}

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
});

export default HomeListScreen;
